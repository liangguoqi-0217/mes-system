# 采购申请批量导入 PRD

---

## 1. 功能背景

用户需要一次性申请多个物料时，逐条手工创建效率太低。本功能提供「下载 Excel 模板 → 线下批量填写 → 上传导入」的创建方式。

核心原则：**先校验、后创建**。系统对上传的每一行数据做完整校验，只要有一行不通过就不允许提交采购申请；用户必须根据错误提示回到 Excel 修改后重新上传，直到全部正确才开放「提交」按钮。

---

## 2. 业务流程

```
下载模板 → 线下填写 → 上传 Excel
                          ↓
                    系统逐行校验
                          ↓
        有错 ──→ 界面展示错误明细（行号 + 列 + 原因 + 修改建议）+ 提交置灰
         │                    ↓
         │              用户修改 Excel 重新上传
         └───────────────────↑
        无错 → 打开采购申请预览表单 → 用户点击「提交」→ 调 SAP 创建接口
```

| 步骤 | 说明 |
|---|---|
| 1 | 点击「模板批导」→ 弹出导入弹窗 |
| 2 | 点击「下载 Excel 模板」，模板含 2 个 Sheet 页 |
| 3 | 按模板填写后点击或拖拽上传（.xlsx / .xls） |
| 4 | 系统解析并逐行校验，返回校验结果 |
| 5a | 存在错误：展示错误明细，**「提交」按钮置灰**，提供「重新上传」入口 |
| 5b | 全部正确：展示数据预览，用户确认后点击「提交」创建采购申请 |
| 6 | 按现有逻辑调 SAP 创建接口，返回采购申请号并写入 EBAN |

---

## 3. 模板结构

模板文件：`采购申请批导模板.xlsx`

| Sheet | 名称 | 用途 | 列范围 | 预置数据行 |
|---|---|---|---|---|
| 1 | 正常采购申请（有 SAP 物料号） | 有物料号的常规采购 | A ~ O | 第 5 ~ 22 行 |
| 2 | 费用化采购申请（无 SAP 物料号） | 无物料号，按物料组+成本中心记账 | A ~ P | 第 5 ~ 21 行 |

### 3.1 行结构（两个 Sheet 一致）

| 行号 | 内容 | 系统处理 |
|---|---|---|
| 第 1 行 | 标题「采购申请表」（整行合并） | 不读取 |
| 第 2 行 | 抬头区：「部门：」（A2:D2 合并）、「日期：」（E2:O2 合并） | **不读取**，部门按登录用户自动带出，日期取上传当天 |
| 第 3 行 | 列标题行（字段名） | 仅用于列定位校验 |
| 第 4 行 | 填写说明行（必填 / 选填 / 格式提示） | 不读取 |
| **第 5 行起** | **数据区，系统从第 5 行开始取数** | 逐行校验 |
| 末尾 | **合计行**（A 列「合计」，A:H 合并，总价列为 `=SUM(...)`） | **不读取、不校验、不传 SAP** |

### 3.2 关键解析规则

| 编号 | 规则 |
|---|---|
| P-01 | 从第 5 行开始逐行读取，遇到合计行即停止（见 3.4） |
| P-02 | A 列序号为公式 `=ROW()-4`，**系统不读取**，行项目号由系统按有效数据行顺序重新生成 10、20、30… |
| P-03 | 空行（除序号外关键列全空）直接跳过，不参与校验、不生成行项目、不占用行号 |
| P-04 | 第 3 行列标题必须与模板一致；若表头行缺失或列名被修改，直接报「模板版本不正确，请重新下载模板」并终止 |
| P-05 | 每个 Sheet 的有效数据行 ≤ 100 行（一个 Sheet = 一张采购申请，沿用单据行项目上限），超出提示「数量太多，请分开建单」 |
| P-06 | 文本统一去首尾空格；数值列去除千分位逗号；物料号、工厂、成本中心按文本处理，保留前导零 |
| P-07 | 所有长度按**字符数**计算，1 个汉字 = 1 个字符（与 SAP CHAR 长度口径一致） |

### 3.3 导入 Sheet 页的确定规则（重点）

**一次导入只处理一个 Sheet 页**：以用户保存/关闭 Excel 时最后停留（激活）的 Sheet 页为准，另一个 Sheet 页的数据一律不读取、不校验、不传 SAP。

| 编号 | 规则 |
|---|---|
| P-08 | 同一 Sheet 内所有数据行的工厂（B 列）必须一致；出现 2 个及以上不同工厂时本次导入失败，提示：`{Sheet名} 中存在多个工厂（{工厂列表}），一个 Sheet 页只能填写一个工厂，请拆分后分别导入` |
| P-09 | 取 xlsx 中记录的激活页签（`workbook.xml` → `bookViews/workbookView@activeTab`）作为本次导入的 Sheet 页；`.xls` 同理取对应窗口记录的当前页签 |
| P-10 | 界面须明确告知用户本次导入的是哪个 Sheet 页：`检测到你最后停留在「{Sheet名}」，本次将导入该 Sheet 页的数据`；若不是预期页签，用户自行切换后重新保存再上传 |
| P-11 | 读取不到激活页签信息时（个别第三方工具导出的文件可能丢失该属性），默认取**第一个有有效数据行**的 Sheet 页并按 P-10 提示告知；**任何情况下不得默认全量导入两个 Sheet** |
| P-12 | 被选中的 Sheet 页无任何有效数据行时，导入失败：`本次将导入的 Sheet 页「{Sheet名}」未填写数据，请切换到实际填写数据的 Sheet 页，保存后重新上传` |
| P-13 | 一个 Sheet 页对应一张采购申请：Sheet 1 → 科目分配类别「空（正常物料采购）」；Sheet 2 → 「K（费用化采购）」 |
| P-14 | 单据抬头：工厂取该 Sheet 的工厂值（P-08 已保证唯一）；部门取登录用户部门；日期取上传当天 |

> 说明：此规则依赖 Excel 的保存行为，用户必须在**停留在目标 Sheet 页的状态下保存**文件。模板的「填写说明 Sheet」（如有）与界面文案均需提示这一点。

### 3.4 合计行识别（重点）

合计行位置**不固定**：用户在数据区中间插入/删除行时，合计行会随之上下移动，因此**禁止写死行号**（如"第 23 行"）。

判定规则（从起始行向下扫描，命中任意一条即判定为合计行）：

1. A 列值去除空格后等于 `合计` / `总计` / `小计`（含全角、冒号变体）；
2. 该行「总价」列单元格为公式且以 `=SUM(` 开头（读公式而非缓存值）；
3. A 列含「合计」关键字，且该行 B~H 列全空。

处理规则：

- 命中合计行 → **立即终止本 Sheet 读取**，其后所有行一律不解析（防止尾部批注被误读为数据）；
- 合计行不参与校验、不生成行项目、不计入行数、不传 SAP；
- 未识别到合计行时，读到最后一个有数据的行为止，**不因此报错**；
- 总价列用公式读取（`data_only=False`），避免缓存值未刷新导致误判。

---

## 4. Sheet 1：正常采购申请（有 SAP 物料号）

### 4.1 字段定义与校验总表

| 列 | 字段名 | 必输 | 类型 / 长度 | 校验规则 |
|---|---|---|---|---|
| A | 序号 | — | — | 不读取，系统生成 10/20/30… |
| B | 工厂 | 必填 | CHAR 4 | 值域校验 + 用户权限校验（4.2） |
| C | 物料号 | 必填 | CHAR 18 | MARC 存在性 + 删除标记（4.3） |
| D | 物料描述 | 必填 | 文本 | 与 MAKT 或 ZLTEXT 比对（4.4） |
| E | 申请人 | 必填 | 文本 ≤ 12 个字符 | 长度校验 |
| F | 数量 | 必填 | 数值，≤ 3 位小数 | 数值 + 小数位 + > 0 |
| G | 单位 | 必填 | UNIT 3 | 与 MEINS 或 BSTME 比对（4.5） |
| H | 评价价格 | 必填 | 金额，≤ 2 位小数 | 数值 + 小数位 |
| I | 总价 | 必填 | 金额，≤ 2 位小数 | 须等于 F × H（允许 0.01 舍入误差） |
| J | 交货日期 | 必填 | 日期 | 格式 + 不早于当前日期（4.6） |
| K | 采购原因 | 选填 | CHAR 100 | 长度校验 |
| L | 使用/库存 | 选填 | CHAR 20 | 长度校验 |
| M | 图片 | 选填 | 单元格嵌入图片 | 见第 6 节 |
| N | 备注 | 选填 | CHAR 100 | 长度校验 |
| O | 预算出处 | 选填 | CHAR 100 | 长度校验 |

### 4.2 B 列 工厂（双重校验）

| 校验 | 逻辑 | 错误提示文案 |
|---|---|---|
| ① 值域 | 存在于 `z_configure_werks.werks_code` | 第 {行} 行：工厂【{值}】不存在于系统工厂值域，请检查填写或联系管理员维护工厂数据 |
| ② 权限 | 当前登录用户拥有该工厂权限 | 第 {行} 行：当前用户无工厂【{值}】的权限，请更换工厂或联系管理员开通 |

① 不通过时不再执行 ②；两种原因必须分开提示，不能只报「工厂错误」。

### 4.3 C 列 物料号

查询条件：`MARC` 按 **工厂（B 列）+ 物料号（C 列）** 组合查询。

| 校验 | 逻辑 | 错误提示文案 |
|---|---|---|
| ① 存在性 | 存在该 工厂+物料 记录 | 第 {行} 行：物料【{值}】在工厂【{工厂}】下不存在，请核对物料号与工厂是否匹配 |
| ② 删除标记 | `MARC-LVORM <> 'X'` | 第 {行} 行：物料【{值}】已打删除标记，不可采购，请检查数据 |

### 4.4 D 列 物料描述

按顺序比对，任一相等即通过：① `MARA-MAKT` → ② `MARA-ZLTEXT` → 都不等则报错。

错误提示：`第 {行} 行：请检查物料描述，与系统物料描述不匹配`

### 4.5 G 列 单位

按顺序比对，任一相等即通过：① `MARA-MEINS` → ② `MARA-BSTME` → 都不等则报错。

错误提示：`第 {行} 行：请检查该物料的单位`

### 4.6 J 列 交货日期

- **格式**：以模板为准，标准格式 `YYYYMMDD`。解析层做容错，同时接受 `YYYYMMDD`、`YYYY/MM/DD`、`YYYY-MM-DD`；单元格本身为日期格式时直接取日期值。无法解析时报格式错误，文案统一提示按 `YYYYMMDD` 填写。
- **业务校验**：不早于当前日期（等于当天允许）。
- 统一转 `YYYYMMDD` 存储与传 SAP。

### 4.7 长度 / 格式类错误文案

| 列 | 错误提示文案 |
|---|---|
| E | 第 {行} 行：申请人超出 12 个字符长度限制，请精简后重新填写 |
| F | 第 {行} 行：数量必须为数字且小数位不超过 3 位，请检查 |
| H | 第 {行} 行：评价价格必须为数字且小数位不超过 2 位，请检查 |
| I | 第 {行} 行：总价必须等于 数量 × 评价价格 且小数位不超过 2 位，请检查 |
| J | 第 {行} 行：交货日期格式错误，请按 YYYYMMDD 填写 |
| J | 第 {行} 行：交货日期不能早于当前日期，请检查 |
| K | 第 {行} 行：采购原因超出 100 个字符长度限制 |
| L | 第 {行} 行：使用/库存超出 20 个字符长度限制 |
| N | 第 {行} 行：备注超出 100 个字符长度限制 |
| O | 第 {行} 行：预算出处超出 100 个字符长度限制 |
| 任意必填列 | 第 {行} 行：{字段名}为必填项，请填写 |

---

## 5. Sheet 2：费用化采购申请（无 SAP 物料号）

### 5.1 字段定义与校验总表

| 列 | 字段名 | 必输 | 类型 / 长度 | 校验规则 |
|---|---|---|---|---|
| A | 序号 | — | — | 同 Sheet 1，不读取 |
| B | 工厂 | 必填 | CHAR 4 | **同 Sheet 1 B 列**（值域 + 权限） |
| C | 物料组 | 必填 | CHAR 9 | 值域校验：存在于 `T023.MATKL` |
| D | 短文本 | 必填 | 文本 ≤ 40 个字符 | 手工填写，长度校验 |
| E | 申请人 | 必填 | 文本 ≤ 12 个字符 | 同 Sheet 1 E 列 |
| F | 数量 | 必填 | 数值，≤ 3 位小数 | 同 Sheet 1 F 列 |
| G | 单位 | 必填 | 文本 ≤ 3 个字符 | 必填 + 长度校验（Sheet 2 无物料号，不做 MEINS / BSTME 比对） |
| H | 评价价格 | **选填** | 金额，≤ 2 位小数 | 同 Sheet 1 H 列 |
| I | 总价 | 必填 | 金额，≤ 2 位小数 | 见 5.2 |
| J | 成本中心 | 必填 | 10 位数字编码 | 值域校验：存在于 `CSKS.KOSTL` |
| K | 交货日期 | 必填 | 日期 | 同 Sheet 1 J 列 |
| L | 采购原因 | 选填 | CHAR 100 | 同 Sheet 1 K 列 |
| M | 使用/库存 | 选填 | CHAR 20 | 同 Sheet 1 L 列 |
| N | 图片 | 选填 | 单元格嵌入图片 | 同 Sheet 1 M 列 |
| O | 备注 | 选填 | CHAR 100 | 同 Sheet 1 N 列 |
| P | 预算出处 | 选填 | CHAR 100 | 同 Sheet 1 O 列 |

### 5.2 I 列总价

与 Sheet 1 保持完全一致：必填、金额、小数位 ≤ 2、须等于 `数量 × 评价价格`（允许 0.01 舍入误差）。

模板中 H 列评价价格标注为选填，H 为空时无法参与乘算，此时仅校验总价为数值且小数位 ≤ 2，不校验乘算。

### 5.3 C 列 物料组

错误提示：`第 {行} 行：物料组【{值}】不存在于系统物料组主数据（T023），请检查`

### 5.4 J 列 成本中心

| 校验 | 逻辑 | 错误提示文案 |
|---|---|---|
| ① 格式 | 10 位数字编码 | 第 {行} 行：成本中心为 10 位数字编码，请检查 |
| ② 值域 | 存在于 `CSKS.KOSTL` | 第 {行} 行：成本中心【{值}】不存在于系统成本中心主数据（CSKS），请检查 |

### 5.5 其他错误文案

| 列 | 错误提示文案 |
|---|---|
| D | 第 {行} 行：短文本为必填项，请描述采购内容 |
| D | 第 {行} 行：短文本超出 40 个字符长度限制 |
| I | 第 {行} 行：总价必须等于 数量 × 评价价格 且小数位不超过 2 位，请检查 |
| I | 第 {行} 行：总价必须为数字且小数位不超过 2 位，请检查 |

---

## 6. M / N 列（图片）

**唯一要求：图片必须「嵌入到单元格」**（模板填写说明：插入 → 图片 → 嵌入到单元格）。

对用户的约束仅此一条，模板与界面提示均按此表述。图片的解析、存储与展示方案由开发自行决定，本 PRD 不做限制。

开发参考（非强制）：

- 嵌入单元格图片与浮动图片的解析方式不同，按所用组件的锚点/行列信息定位到数据行即可；
- 图片随行暂存，校验通过且用户提交后随采购申请一并保存；校验未通过时不落库。

---

## 7. 通用业务规则

| 编号 | 规则 |
|---|---|
| BR-B01 | 第 1~4 行及抬头区不读取，从第 5 行开始取数 |
| BR-B02 | A 列序号不读取，行项目号按有效数据行顺序生成 10、20、30… |
| BR-B03 | 合计行按 3.3 规则动态识别，不读取、不校验、不传 SAP |
| BR-B04 | 存在任一错误行时不允许提交，「提交」按钮置灰并提示错误行数 |
| BR-B05 | 修改数据必须回到 Excel 修改后重新上传；导入结果界面为只读预览，不支持在线编辑修正 |
| BR-B06 | 错误提示必须包含：行号 + 出错列 + 具体原因 + 可执行的修改建议 |
| BR-B07 | 单行多个错误全部返回，不中断后续行校验，一次性展示所有问题 |
| BR-B08 | 每个 Sheet 有效数据行 ≤ 100 行，超出提示「数量太多，请分开建单」 |
| BR-B09 | 空行跳过，不生成行项目；表头校验失败直接终止并提示重新下载模板 |
| BR-B10 | 校验全部通过前不调用 SAP 接口、不产生任何单据数据 |
| BR-B11 | 同一 Sheet 内不允许多个工厂；一次导入仅处理用户最后停留的 Sheet 页，一个 Sheet 页对应一张采购申请（见 3.3） |

---

## 8. 错误展示要求（前端）

1. **汇总条**：顶部红色横幅 `共 N 行，其中 M 行存在错误，请修改 Excel 后重新上传`。
2. **逐行标注**：错误行整行浅红底 + 左侧红色竖线。
3. **错误明细列**：表格最右侧增加「校验说明」列，展示该行全部错误；超过 1 条时折叠为「第一条（等 N 项）」，悬浮显示全部。
4. **定位辅助**：错误文案中的行号同时给出 **系统行项目号** 与 **Excel 物理行号**，例如 `行项目 20（Excel 第 6 行）`，方便用户回 Excel 定位。
5. **入口**：底部保留「重新上传」按钮，点击回到导入弹窗。
6. **Sheet 提示**：结果区顶部标注本次实际导入的 Sheet 页名称（如「本次导入：费用化采购申请（无 SAP 物料号）」），不需按 Sheet 分页展示。

---

## 9. 已确认事项

| 序号 | 事项 | 结论 |
|---|---|---|
| 1 | 日期格式 | 以模板为准，标准格式 `YYYYMMDD`；解析层容错 `YYYY/MM/DD`、`YYYY-MM-DD` |
| 2 | 图片列 | 唯一要求：图片必须嵌入到单元格；解析与存储方案由开发自行实现 |
| 3 | 多工厂与导入范围 | 同一 Sheet 不允许多个工厂；一次导入只处理用户保存时最后停留的 Sheet 页；一个 Sheet 页 = 一张采购申请 |
| 4 | Sheet 2 总价 | 与 Sheet 1 逻辑一致 |
| 5 | 抬头区「部门」「日期」 | 不读取，部门取登录用户部门，日期取上传当天 |
| 6 | 字段长度口径 | 按字符数计算，1 个汉字 = 1 个字符（与 SAP CHAR 口径一致） |

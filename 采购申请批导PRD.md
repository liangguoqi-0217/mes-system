# 采购申请批量导入 PRD

---

## 1. 功能背景

用户需要一次性申请多个物料时，逐条手工创建效率太低。本功能提供「下载 Excel 模板 → 线下批量填写 → 上传导入」的创建方式。

核心原则：**先校验、后创建**。系统对上传的每一行数据做完整校验，只要有一行不通过，就不允许提交采购申请；用户必须根据错误提示修改 Excel 后重新上传，直到全部正确才开放「提交」按钮。

---

## 2. 业务流程

```
下载模板 → 线下填写 → 上传 Excel
                          ↓
                   系统逐行校验
                          ↓
              有错 ────────────→ 界面展示错误明细（行号 + 列 + 原因 + 修改建议）
               │                        ↓
               │                  用户修改 Excel 重新上传
               │                        ↓
               └────────────────────────↑
              无错 → 打开采购申请预览表单 → 用户点击「提交」→ 调 SAP 创建接口
```

| 步骤 | 说明 |
|---|---|
| 1 | 用户点击「模板批导」→ 弹出导入弹窗 |
| 2 | 点击「下载 Excel 模板」，模板含 2 个 Sheet 页 |
| 3 | 用户按模板填写后，点击或拖拽上传（.xlsx / .xls） |
| 4 | 系统解析并逐行校验，返回校验结果 |
| 5a | 存在错误：展示错误明细，**提交按钮置灰不可点**，提供「重新上传」入口 |
| 5b | 全部正确：展示数据预览，用户确认后点击「提交」创建采购申请 |
| 6 | 提交后按现有逻辑调 SAP 创建接口，返回采购申请号并写入 EBAN |

---

## 3. 模板结构

| 项 | 规则 |
|---|---|
| Sheet 1 | 正常采购申请（有 SAP 物料号） |
| Sheet 2 | 费用化采购申请（无 SAP 物料号） |
| 分 Sheet 原因 | 两类业务字段必输要求不同，分开可简化校验逻辑，避免大量条件判断 |
| 数据起始行 | **第 5 行**。第 1~4 行为标题与合并表头，系统不读取 |
| 行号规则 | A 列序号仅供用户查看，**系统不读取**；导入后由系统按 10、20、30… 重新生成行项目号（BNFPO） |
| 行数限制 | 建议单次导入合计 ≤ 100 行，超出提示「数量太多，请分开建单」 |
| 空行处理 | 关键列全空的行直接跳过，不参与校验、不生成行项目 |

---

## 4. Sheet 1：正常采购申请字段校验规则

> 列以模板实际列序为准；字段名标注「待确认」的，请以模板表头最终名称填写。

### 4.1 字段总表

| 列 | 字段 | 必输 | 类型/长度 | 校验规则 |
|---|---|---|---|---|
| A | 序号 | — | — | **不读取**，系统重新生成 10/20/30… |
| B | 工厂 | 是 | CHAR 4 | 值域校验 + 用户权限校验（见 4.2） |
| C | 物料号 | 是 | CHAR 18 | MARC 存在性校验 + 删除标记校验（见 4.3） |
| D | 物料描述 | 是 | 文本 | 与 MAKT 或 ZLTEXT 比对（见 4.4） |
| E | （待确认） | 是 | 文本 ≤ 12 汉字 | 长度校验 |
| F | 申请数量 | 是 | 数值，≤ 3 位小数 | 数值 + 小数位 + > 0 |
| G | 单位 | 是 | UNIT 3 | 与 MEINS 或 BSTME 比对（见 4.5） |
| H | 单价 | 是 | 金额，≤ 2 位小数 | 数值 + 小数位 |
| I | 金额 | 是 | 金额，≤ 2 位小数 | 须等于 F × H（允许 0.01 四舍五入误差） |
| J | 交货日期 | 是 | 日期 YYYY/MM/DD | 格式校验 + 不得早于当前日期 |
| K | （待确认） | 否 | CHAR 100 | 长度校验 |
| L | （待确认） | 否 | CHAR 20 | 长度校验 |
| M | 图片 | 否 | 单元格嵌入图片 | 见第 6 节建议 |
| N | （待确认） | 否 | CHAR 100 | 长度校验 |
| O | （待确认） | 否 | CHAR 100 | 长度校验 |

### 4.2 B 列 工厂（双重校验）

| 校验 | 逻辑 | 错误提示文案 |
|---|---|---|
| ① 值域 | 工厂代码存在于 `z_configure_werks.werks_code` | 第 {行号} 行：工厂【{值}】不存在于系统工厂值域，请检查填写或联系管理员维护工厂数据 |
| ② 权限 | 当前登录用户拥有该工厂权限 | 第 {行号} 行：当前用户无工厂【{值}】的权限，请更换工厂或联系管理员开通 |

> ① 不通过时不再执行 ②；两条校验的错误原因必须区分展示，不能只报「工厂错误」。

### 4.3 C 列 物料号

查询条件：`MARC` 表按 **工厂（B列）+ 物料号（C列）** 组合查询。

| 校验 | 逻辑 | 错误提示文案 |
|---|---|---|
| ① 存在性 | MARC 中存在该 工厂+物料 记录 | 第 {行号} 行：物料【{值}】在工厂【{工厂}】下不存在，请核对物料号与工厂是否匹配 |
| ② 删除标记 | `MARC-LVORM <> 'X'` | 第 {行号} 行：物料【{值}】已打删除标记，不可采购，请检查数据 |

### 4.4 D 列 物料描述

按顺序比对，任一相等即通过：

1. 与 `MARA-MAKT` 相等 → 通过
2. 否则与 `MARA-ZLTEXT` 相等 → 通过
3. 都不相等 → 报错

错误提示：`第 {行号} 行：请检查物料描述，与系统物料描述不匹配`

### 4.5 G 列 单位

按顺序比对，任一相等即通过：

1. 与 `MARA-MEINS` 相等 → 通过
2. 否则与 `MARA-BSTME` 相等 → 通过
3. 都不相等 → 报错

错误提示：`第 {行号} 行：请检查该物料的单位`

### 4.6 长度 / 格式类校验错误文案

| 列 | 错误提示文案 |
|---|---|
| E | 第 {行号} 行：【{字段名}】超出 12 个汉字长度限制，请精简后重新填写 |
| F | 第 {行号} 行：申请数量必须为数字且小数位不超过 3 位，请检查 |
| H | 第 {行号} 行：单价必须为数字且小数位不超过 2 位，请检查 |
| I | 第 {行号} 行：金额必须等于 数量 × 单价 且小数位不超过 2 位，请检查 |
| J | 第 {行号} 行：交货日期格式错误，请按 YYYY/MM/DD 填写 |
| J | 第 {行号} 行：交货日期不能早于当前日期，请检查 |
| K | 第 {行号} 行：【{字段名}】超出 100 字符长度限制 |
| L | 第 {行号} 行：【{字段名}】超出 20 字符长度限制 |
| N | 第 {行号} 行：【{字段名}】超出 100 字符长度限制 |
| O | 第 {行号} 行：【{字段名}】超出 100 字符长度限制 |

**日期解析规则**：单元格可能是 Excel 日期序列号或文本。先按数值序列号转换，再按字符串匹配 `^\d{4}\/\d{1,2}\/\d{1,2}$`，两者都失败才报格式错误；格式通过后统一转为 `YYYYMMDD` 存储与传 SAP。

---

## 5. Sheet 2：费用化采购申请

> 待补充（请继续叙述列定义与校验规则）。

已知边界：费用化采购**无 SAP 物料号**，因此不做 B/C/D/G 列相关的物料主数据校验，改由物料组、短文本、成本中心等字段承担信息描述。

---

## 6. M 列（单元格嵌入图片）校验建议

嵌入单元格的图片在解析层面成本较高，给出三个方案，推荐 A：

| 方案 | 做法 | 评价 |
|---|---|---|
| **A（推荐）** | **放弃解析 Excel 内嵌图片**。M 列在模板中改为「图片名称」文本列（如 `IMG001`），用户填写名称，导入后在界面单独批量上传图片附件，按名称与行匹配，导入结果与附件一并提交 | 实现最稳、与现有附件机制一致；用户操作多一步 |
| B | 后端用 POI 读取 XSSF drawing 锚点定位图片所属行，校验：是否为空、格式（jpg/png）、单张 ≤ 2MB、每单元格限 1 张 | 体验最好，但需处理浮动图片锚点漂移、多图重叠等边界情况，开发与测试成本高 |
| C | 完全不校验、不存储，界面仅提示「如需附图请在提交后单独上传」 | 最省事，但等于该列无业务价值 |

若坚持方案 B，建议的最小校验集：图片存在性 → 格式白名单（jpg/jpeg/png）→ 大小上限 → 锚点行归属（无法定位到行的图片整体报错 `第 X 行：图片未正确嵌入单元格，请按模板示例插入`）。

---

## 7. 通用规则

| 编号 | 规则 |
|---|---|
| BR-B01 | 第 1~4 行不取数，从第 5 行开始解析 |
| BR-B02 | A 列序号不读取，行项目号由系统生成 10、20、30… |
| BR-B03 | 校验不通过时不允许提交，「提交」按钮置灰，提示未通过行数 |
| BR-B04 | 修改数据必须回到 Excel 修改后重新上传，导入结果界面为只读预览，不支持在线编辑修正 |
| BR-B05 | 错误提示必须包含：行号 + 出错列 + 具体原因 + 可执行的修改建议 |
| BR-B06 | 单行存在多个错误时全部返回，不中断后续行校验，一次性展示所有问题 |
| BR-B07 | 行项目上限 100 行，超出提示「数量太多，请分开建单」 |
| BR-B08 | 关键列全空的空行跳过，不生成行项目、不计入错误 |
| BR-B09 | 数据类型统一：导入前去除首尾空格；数值列去除千分位逗号；前导零物料号按文本处理 |

---

## 8. 错误展示要求（前端）

1. **汇总条**：顶部红色横幅，`共 N 行，其中 M 行存在错误`，并说明「请修改 Excel 后重新上传」。
2. **逐行标注**：错误行整行浅红底 + 左侧红色竖线。
3. **错误明细列**：表格最右侧增加「校验说明」列，展示该行全部错误，超过 1 条时折叠为「第一条（等 N 项）」，悬浮显示全部。
4. **定位辅助**：错误文案中的行号同时给出 **系统行项目号** 与 **Excel 物理行号**（如 `行项目 20（Excel 第 6 行）`），方便用户回 Excel 定位。
5. **入口**：底部保留「重新上传」按钮，点击回到导入弹窗。

---

## 9. 待确认事项

| 序号 | 事项 |
|---|---|
| 1 | E、K、L、N、O 列的正式字段名与是否必输 |
| 2 | Sheet 2（费用化采购）全部列定义与校验规则 |
| 3 | M 列图片方案选型（A / B / C） |
| 4 | 一次导入中若存在多个工厂，是否按工厂拆分为多张采购申请 |
| 5 | 金额 I 列是否允许用户在容差内手工调整（当前按 0.01 四舍五入容差处理） |
| 6 | 交货日期是否允许等于当天（当前按允许处理） |

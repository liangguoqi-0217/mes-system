# 查询变式（我的变式）功能 PRD

> 版本：V1.2（表结构为主表 + 明细表；已定稿：ZMOREN 允许 NULL、上限 20、物理删除、正序排列、本期仅 EQ）
> 适用：MES V3 全部带查询条件的页面
> 示例：以「备件库存查询」页面进行说明
> 作者：产品侧口述 / AI 整理

---

## 一、功能概述

**查询变式**（又称「我的变式」）是 MES V3 的**全局框架能力**，用于保存用户常用的筛选条件组合。用户在任一查询页面上输入一组条件后，可将其保存为一个「变式」；后续进入该页面时，可一键调用变式，自动回填条件并执行查询，避免每次重复录入。

- **全局性**：一套组件 + 一套存储规则，统一接入所有带查询条件的界面。
- **个人化**：变式按「用户 + 程序名」隔离，仅创建者本人可见、可用。
- **默认唯一**：同一用户、同一程序下**只允许有一个默认变式**。

---

## 二、适用范围

适用于 MES V3 中所有具备筛选栏的列表/查询页面，包括但不限于：

- 备件库存查询
- 维修工单查询
- 采购申请查询
- 备件领用查询
- 其他需要保存常用查询条件的页面

---

## 三、数据表设计

采用**主表 + 明细表**结构：主表存变式本身的属性（编码、描述、程序、是否默认、创建信息），明细表存该变式下的每个查询字段条件。

### 3.1 主表：`ZBIANSHI_H`（变式头表）

一个变式在主表中只存**一行**。

| 序号 | 字段名 | 字段描述 | 字符类型 | 字符长度 | 是否主键 | 字段值来源 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `ZBIANSHI` | 变式编码 | CHAR | 100 | 是 | 系统自动生成 | 举例：`liangguoqi-20260923-112301` |
| 2 | `ZBIANSHIMIAOSHU` | 变式描述 | CHAR | 50 | 否 | 用户输入 | 举例：查询 A 库位冻结库存 |
| 3 | `ZCHENGXUMING` | 程序名 | CHAR | 100 | 否 | 系统自动抓取 | 当前界面所在程序的程序名 |
| 4 | `ZMOREN` | 是否默认变式 | CHAR | 1 | 否 | 系统逻辑写入 | `X` = 默认变式；空 = 非默认 |
| 5 | `ZUSER` | 用户名 | CHAR | 40 | 否 | 当前登录用户 | 举例：`admin` |
| 6 | `CREATIONDATE` | 创建日期 | DATS | 8 | 否 | 系统生成 | 举例：`20260923` |
| 7 | `CREATIONTIME` | 创建时间 | TIME | 6 | 否 | 系统生成 | 举例：`112301` |

**主键**：`ZBIANSHI`（变式编码已包含用户名 + 日期时间，天然全局唯一）

### 3.2 明细表：`ZBIANSHI_D`（变式行表）

一个变式的每个查询字段在明细表中存**一行**。

| 序号 | 字段名 | 字段描述 | 字符类型 | 字符长度 | 是否主键 | 字段值来源 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `ZBIANSHI` | 变式编码 | CHAR | 100 | 是 | 关联主表 | 与主表 `ZBIANSHI` 一致 |
| 2 | `ZIZIDUAN` | 字段 | CHAR | 50 | 是 | 查询条件 | 举例：`LGORT` |
| 3 | `ZOPTION` | 选项 | CHAR | 2 | 否 | 默认 EQ | 值域：EQ/NE/GT/LT/GE/LE/BT/CP，目前仅实现 `EQ` |
| 4 | `ZLOW` | 字段值下限 | CHAR | 255 | 否 | 用户输入 | 单值时存下限即可；举例：`1001` |
| 5 | `ZHIGH` | 字段值上限 | CHAR | 255 | 否 | 用户输入 | 范围值（如日期）需存上下限 |

**主键**：`ZBIANSHI` + `ZIZIDUAN`（同一变式下同一字段只能有一条条件记录）

### 3.3 索引与约束建议

```sql
-- 主表：按「用户 + 程序」查询变式列表的高频场景
CREATE INDEX idx_h_user_prog ON ZBIANSHI_H (ZUSER, ZCHENGXUMING);

-- 主表：保证同一用户同一程序下只有一个默认变式
-- 依赖 NULL 不参与唯一性冲突：非默认行 ZMOREN 为 NULL 可多行并存，
-- 只有 ZMOREN='X' 的行之间才会冲突，从而约束为唯一一条
CREATE UNIQUE INDEX uk_h_default ON ZBIANSHI_H (ZUSER, ZCHENGXUMING, ZMOREN);

-- 主表：同一用户同一程序下变式描述不重复
CREATE UNIQUE INDEX uk_h_name ON ZBIANSHI_H (ZUSER, ZCHENGXUMING, ZBIANSHIMIAOSHU);

-- 明细表：按变式编码查询其全部条件行
CREATE INDEX idx_d_code ON ZBIANSHI_D (ZBIANSHI);
```

> **已确认**：`ZMOREN` 字段**允许为 NULL**，上述唯一索引 `uk_h_default` 生效。规则为：
> - 非默认行 `ZMOREN` 存 `NULL`（多条 NULL 互不冲突，可并存任意条）
> - 默认行 `ZMOREN` 存 `X`（唯一索引保证同一用户同一程序下只能有一条）
>
> 由此在**数据库层**强制保证「默认唯一」，无需依赖应用层。
>
> **开发注意**：非默认必须存 `NULL`，**不可存空字符串 `''`**，否则多条空字符串会被判定为重复值导致插入/更新报错。应用层切换默认时仍按 6.3 的两步 UPDATE 执行（先全清为 NULL、再置 X）作为一致性兜底。

## 四、字段业务规则

### 4.1 变式编码（`ZBIANSHI`）

- **自动生成**，用户不可修改。
- **生成规则**：`用户登录名-创建日期-创建时间`，例如 `liangguoqi-20260923-112301`。
- 日期格式 `YYYYMMDD`，时间格式 `HHMMSS`。
- 生成时机：打开「我的变式」弹窗时生成，点保存时落库；保存成功后立即生成新编码，避免连续保存撞号。
- 若同一秒内保存多个变式，后端需保证唯一性，可追加序号 `-2`、`-3`。

### 4.2 变式描述（`ZBIANSHIMIAOSHU`）

- 用户手工输入，**必填**，最大长度 50 字符。
- 同一用户、同一程序下不可重复。

### 4.3 程序名（`ZCHENGXUMING`）

- 系统自动抓取当前页面所属程序名，用于隔离不同页面的变式。

### 4.4 是否默认变式（`ZMOREN`）

- 标识该变式是否为当前用户在该程序下的**默认变式**（主表字段，只存一行，无冗余）。
- 字段**允许为 NULL**。取值：`X` = 默认；`NULL` = 非默认（**不可存空字符串**）。
- **约束：同一用户、同一程序下有且仅有一个变式的 `ZMOREN = 'X'`。**
- 该约束由唯一索引 `uk_h_default (ZUSER, ZCHENGXUMING, ZMOREN)` 在数据库层强制保证。
- 切换默认时：先清空该用户该程序下所有变式的 `ZMOREN`，再给目标变式置 `X`（同一事务）。
- 作用：用户下次进入该页面时，自动回填默认变式的条件。

### 4.5 字段（`ZIZIDUAN`）

- 来自当前程序查询条件的字段名。
- 保存变式时，把当前页面所有有值、未置灰、未隐藏的查询字段逐条写入明细表。

### 4.6 选项（`ZOPTION`）

- **本期范围：仅实现 `EQ`（等于）**，所有明细行固定写入 `EQ`，界面不提供操作符选择控件。
- **后续版本**再考虑扩展：`NE` 不等于、`GT` 大于、`LT` 小于、`GE` 大于等于、`LE` 小于等于、`BT` 介于之间、`CP` 包含（模糊搜索）。本期不做 `BT`/`CP` 的界面配置与后端解析。

### 4.7 字段值下限 / 上限（`ZLOW` / `ZHIGH`）

- **单值条件**：仅写入 `ZLOW`，`ZHIGH` 为空。
- **范围条件**（如日期区间、数量区间）：`ZLOW` 写入起始值，`ZHIGH` 写入结束值。

### 4.8 用户名 / 创建日期 / 创建时间

- `ZUSER` 取当前登录用户名。
- `CREATIONDATE` / `CREATIONTIME` 取服务器时间落库，分别按 `DATS` / `TIME` 格式存储。

---

## 五、界面设计

### 5.1 入口

每个查询页面的筛选栏「查询」按钮右侧，放置 **「我的变式」** 按钮（`btn btn-secondary btn-sm`）。

```text
[查询] [重置] [我的变式]
```

### 5.2 弹窗整体布局

点击「我的变式」后弹出 `modal-md` 尺寸弹窗，标题为 **「我的查询变式」**，内部采用**上下分区**布局：

```text
┌─────────────────────────────────────────────────────────┐
│ 我的查询变式                                         [×] │
├─────────────────────────────────────────────────────────┤
│ 【已保存的变式】                                         │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 变式编码      │ 变式名称           │ 默认 │ 操作     │ │
│ ├───────────────┼────────────────────┼──────┼──────────┤ │
│ │ liangguoqi-.. │ 周一备件盘库 [当前] │ 默认 │ 已默认 删除│ │
│ │ liangguoqi-.. │ ERSA 紧急件查询    │      │ 设为默认 删除│ │
│ │ ...           │ ...                │      │ ...      │ │
│ └─────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────┤
│ 【保存当前条件为新变式】                                  │
│ 变式编码              变式名称*              设为默认 [保存]│
│ [系统自动生成]        [                   ] [ ] [保存为新变式]│
├─────────────────────────────────────────────────────────┤
│                                    [关闭]                │
└─────────────────────────────────────────────────────────┘
```

### 5.3 已保存变式区

- **展示形式**：表格，表头为「变式编码、变式名称、默认、操作」。
- **排序规则**：按变式创建时间**正序**排列（先保存的在前），不支持拖拽或手动调整顺序，不提供排序号字段。
- **行说明**：
  - 每行对应主表 `ZBIANSHI_H` 的一条记录。
  - `ZMOREN = 'X'` 的行在「默认」列显示「默认」标签，其操作列的「设为默认」按钮替换为不可点的「已默认」。
  - 当前正在使用的变式行高亮，并在名称后显示「当前」标签。
- **行交互**：
  - **点击整行**：应用该变式（回填条件并执行查询），弹窗关闭。
  - **设为默认**：将该变式设为当前页面默认变式。
  - **删除**：二次确认后删除该变式（主表 + 明细表一并处理）。

### 5.4 保存新变式区

- **布局**：单行横向排列，从左到右依次为：
  1. **变式编码**：只读、灰底、不可修改，显示系统自动生成的编码。
  2. **变式名称**：必填输入框。
  3. **设为默认**：复选框，勾选后保存时同时将当前变式设为默认（主表 `ZMOREN = 'X'`，并清空同程序下其他默认）。
  4. **保存为新变式**：按钮，点击保存。

---

## 六、核心业务流程

### 6.1 保存变式

1. 用户在筛选栏输入条件，点击「我的变式」打开弹窗。
2. 系统生成新的变式编码并显示在变式编码框中。
3. 用户输入变式描述，可选择是否设为默认。
4. 点击「保存为新变式」：
   - 校验变式描述非空、同名校验、数量上限校验。
   - **写主表 `ZBIANSHI_H` 一行**：`ZBIANSHI`、`ZBIANSHIMIAOSHU`、`ZCHENGXUMING`、`ZMOREN`、`ZUSER`、`CREATIONDATE`、`CREATIONTIME`。
   - 若设为默认：先清空该用户该程序下所有变式的 `ZMOREN`，再置本条为 `X`。
   - **写明细表 `ZBIANSHI_D` 多行**：每个有值的查询字段写一行，`ZIZIDUAN` 字段名、`ZOPTION='EQ'`、`ZLOW`（单值）或 `ZLOW`+`ZHIGH`（范围值）。
5. 保存成功后刷新上方变式列表，清空名称输入框，变式编码框更新为新编码。

### 6.2 应用变式

1. 用户在弹窗中点击某一行变式。
2. 弹窗关闭，系统按 `ZBIANSHI` 查询明细表 `ZBIANSHI_D` 中该变式的全部字段记录。
3. 将各字段值回填到筛选栏对应控件中（范围字段按 `ZLOW`/`ZHIGH` 分别回填起止控件）。
4. 自动执行查询，并标记该变式为「当前」变式。

### 6.3 设置默认变式

1. 用户在弹窗中点击某行的「设为默认」。
2. 同一事务内执行两条更新：
   - `UPDATE ZBIANSHI_H SET ZMOREN = NULL WHERE ZUSER = ? AND ZCHENGXUMING = ?`
   - `UPDATE ZBIANSHI_H SET ZMOREN = 'X' WHERE ZBIANSHI = ?`
3. 用户下次进入该页面时，系统自动回填默认变式的条件（不自动查询）。

### 6.4 删除变式（物理删除）

1. 用户点击某行的「删除」，弹窗二次确认：「确定删除变式「XXX」吗？删除后不可恢复。」。
2. 确认后执行**物理删除**（不做软删、不设删除标记字段），同一事务内按序执行：
   - `DELETE FROM ZBIANSHI_D WHERE ZBIANSHI = ?`（先删明细表 N 行）
   - `DELETE FROM ZBIANSHI_H WHERE ZBIANSHI = ?`（再删主表 1 行）
3. 若删除的是默认变式，该程序下将无默认变式，下次进入页面不再自动回填。
4. 若删除的是当前正在使用的变式，取消「当前」标记。
5. 删除后数据不可恢复，前端需在二次确认文案中明确提示该后果。

### 6.5 进入页面自动回填

1. 用户进入某查询页面。
2. 系统查找该用户 + 该程序名下 `ZMOREN = 'X'` 的默认变式：
   - 存在：自动回填默认变式条件。
   - 不存在：查找最近一次查询的「上次条件」自动回填（临时自动保存，不展示在变式列表中）。
   - 都没有：保持空白筛选条件。

## 七、业务规则约束

| 规则项 | 说明 |
| --- | --- |
| 编码唯一 | 变式编码全局唯一（含用户名 + 时间戳），作为主表主键 |
| 描述不重复 | 同一用户、同一程序下变式描述不可重复 |
| 默认唯一 | **同一用户、同一程序下有且仅有一个变式的 `ZMOREN = 'X'`** |
| 数量上限 | **每个用户每个程序最多保存 20 个变式**（已确认） |
| 列表排序 | 前端按创建时间**正序**展示（先保存的在前），不支持拖拽或手动排序 |
| 空值过滤 | 保存时不存储未填写、已置灰、已隐藏的字段 |
| 级联删除 | **物理删除**：删除主表变式时必须同步删除明细表该编码下的全部条件行，同一事务，不可恢复 |
| 个人隔离 | 变式按用户名隔离，不可跨用户查看或使用 |
| 程序隔离 | 变式按程序名隔离，不同页面的变式互不干扰 |

---

## 八、前后端接口建议

### 8.1 查询变式列表

```http
GET /api/query-variant/list?program={ZCHENGXUMING}
```

返回当前用户在某程序下保存的全部变式（读主表 `ZBIANSHI_H`），**按创建时间正序排列**（`ORDER BY CREATIONDATE ASC, CREATIONTIME ASC`），先保存的排在前面。不支持用户自定义排序。

### 8.2 保存变式

```http
POST /api/query-variant/save
```

请求体：

```json
{
  "ZBIANSHIMIAOSHU": "查询A库位冻结库存",
  "ZCHENGXUMING": "spare-parts-stock",
  "isDefault": true,
  "conditions": [
    { "ZIZIDUAN": "werks", "ZOPTION": "EQ", "ZLOW": "1000", "ZHIGH": "" },
    { "ZIZIDUAN": "lgort", "ZOPTION": "EQ", "ZLOW": "1001", "ZHIGH": "" },
    { "ZIZIDUAN": "dateFrom", "ZOPTION": "EQ", "ZLOW": "2026-09-01", "ZHIGH": "" }
  ]
}
```

后端落库：主表 `ZBIANSHI_H` 写 1 行，明细表 `ZBIANSHI_D` 按 `conditions` 数组写 N 行，同一事务。

### 8.3 应用变式

```http
GET /api/query-variant/apply/{ZBIANSHI}
```

返回明细表中该变式的全部条件行，供前端回填。

### 8.4 设置默认 / 删除

```http
POST /api/query-variant/set-default/{ZBIANSHI}
POST /api/query-variant/delete/{ZBIANSHI}
```

- 设置默认：同一事务内先将该用户该程序全部 `ZMOREN` 置为 NULL，再置目标为 `X`。
- 删除：**物理删除**，同一事务内先删明细表 N 行、再删主表 1 行，不可恢复。

---

## 九、库存查询示例

### 9.1 页面条件

| 字段名 | 字段说明 | 用户输入值 |
| --- | --- | --- |
| `werks` | 工厂 | `1000` |
| `lgort` | 库存地点 | `1001` |
| `matnr` | 物料号 | `60001018` |
| `dateFrom` | 过账日期起 | `2026-09-01` |
| `dateTo` | 过账日期止 | `2026-09-23` |

### 9.2 主表落库数据（`ZBIANSHI_H`，1 行）

用户 `liangguoqi` 于 `2026-09-23 11:23:01` 保存变式，描述为「查询 A 库位冻结库存」，并设为默认，生成编码 `liangguoqi-20260923-112301`。

| ZBIANSHI | ZBIANSHIMIAOSHU | ZCHENGXUMING | ZMOREN | ZUSER | CREATIONDATE | CREATIONTIME |
|---|---|---|---|---|---|---|
| liangguoqi-20260923-112301 | 查询 A 库位冻结库存 | spare-parts-stock | X | liangguoqi | 20260923 | 112301 |

### 9.3 明细表落库数据（`ZBIANSHI_D`，5 行）

| ZBIANSHI | ZIZIDUAN | ZOPTION | ZLOW | ZHIGH |
|---|---|---|---|---|
| liangguoqi-20260923-112301 | werks | EQ | 1000 | |
| liangguoqi-20260923-112301 | lgort | EQ | 1001 | |
| liangguoqi-20260923-112301 | matnr | EQ | 60001018 | |
| liangguoqi-20260923-112301 | dateFrom | EQ | 2026-09-01 | |
| liangguoqi-20260923-112301 | dateTo | EQ | 2026-09-23 | |

> 注：日期区间目前按两个单值字段 `dateFrom` / `dateTo` 存储。未来若支持 `BT` 选项，可合并为一条明细记录：`ZOPTION=BT`、`ZLOW=2026-09-01`、`ZHIGH=2026-09-23`。

### 9.4 切换默认示例

若用户随后把另一个变式 `liangguoqi-20260923-114510` 设为默认，则主表变化如下（同一事务）：

```sql
UPDATE ZBIANSHI_H SET ZMOREN = NULL
 WHERE ZUSER = 'liangguoqi' AND ZCHENGXUMING = 'spare-parts-stock';

UPDATE ZBIANSHI_H SET ZMOREN = 'X'
 WHERE ZBIANSHI = 'liangguoqi-20260923-114510';
```

明细表无需任何改动——这正是主表 + 明细表结构相对扁平表的优势。

---

## 十、异常处理

| 异常场景 | 处理说明 |
| --- | --- |
| 变式描述为空 | 提示「请输入变式名称」 |
| 同名变式已存在 | 提示「已存在同名变式，请换一个名称」 |
| 超过最大保存数量 | 提示「每个页面最多保存 20 个变式，请先删除不用的」 |
| 变式编码重复 | 提示「变式编码已存在，请关闭弹窗重新打开后再保存」 |
| 删除默认变式 | 删除后该页面无默认变式，下次进入页面不再自动回填 |
| 明细写入失败 | 主表与明细表必须在同一事务，任一步失败则整体回滚 |
| 同一程序出现多个默认 | 数据修复：保留创建时间最早的一条 `ZMOREN='X'`，其余置 NULL |

---

## 十一、附录：前端实现索引

- 前端框架文件：`V3/js/core/query-variant.js`
- 公共样式：`V3/css/common.css`（搜索 `.qv-` 相关样式）
- 示例页面：`V3/js/pages/spare-parts-stock.js`
- 设计 Demo：`V3/demo-query-variant-styles.html`

前端当前以 localStorage 模拟存储，字段映射关系：

| 数据库字段 | 前端字段 |
|---|---|
| `ZBIANSHI` | `variant_code` |
| `ZBIANSHIMIAOSHU` | `variant_name` |
| `ZCHENGXUMING` | `page_id` |
| `ZMOREN` | `is_default` |
| `ZUSER` | `user_id` |
| `ZIZIDUAN` / `ZLOW` | `conditions_json`（JSON 对象） |

---

## 十二、已确认决策（V1.2 定稿）

| # | 决策项 | 结论 | 落地影响 |
| --- | --- | --- | --- |
| 1 | `ZMOREN` 是否允许 NULL | **允许 NULL** | 唯一索引 `uk_h_default` 生效，数据库层强制默认唯一；非默认存 NULL，不可存空字符串 |
| 2 | 是否本期支持 `BT`（区间）/ `CP`（模糊搜索） | **本期不支持** | 明细行固定 `ZOPTION = 'EQ'`，界面不提供操作符选择；列为后续版本规划 |
| 3 | 变式数量上限 | **20 个**（每用户每程序） | 超限时拦截保存并提示 |
| 4 | 删除方式 | **物理删除** | 不设删除标记字段；同一事务先删明细表 N 行、再删主表 1 行；二次确认需提示不可恢复 |
| 5 | 是否支持排序 | **不支持** | 不设排序号字段；列表固定按 `CREATIONDATE`/`CREATIONTIME` 正序（先保存的在前） |

### 后续版本规划（本期不实现）

- `ZOPTION` 扩展：`BT` 区间、`CP` 模糊搜索及其他比较符，含界面操作符选择控件。
- 变式级扩展属性：使用次数、最近使用时间（主表可平滑加字段，不影响明细表）。
- 变式共享：从个人变式（`PERSONAL`）扩展到角色/全局共享变式。

# 查询变式（我的变式）功能 PRD

> 版本：V1.0  
> 适用：MES V3 全部带查询条件的页面  
> 示例：以「备件库存查询」页面进行说明  
> 作者：产品侧口述 / AI 整理  

---

## 一、功能概述

**查询变式**（又称「我的变式」）是 MES V3 的**全局框架能力**，用于保存用户常用的筛选条件组合。用户在任一查询页面上输入一组条件后，可将其保存为一个「变式」；后续进入该页面时，可一键调用变式，自动回填条件并执行查询，避免每次重复录入。

- **全局性**：一套组件 + 一套存储规则，统一接入所有带查询条件的界面。
- **个人化**：变式按「用户 + 程序名」隔离，仅创建者本人可见、可用。
- **业务示例**：库存查询页面可保存「周一备件盘库」「ERSA 紧急件查询」「2001 工厂月度盘点」等变式。

---

## 二、适用范围

适用于 MES V3 中所有具备筛选栏（`filter-bar`）的列表/查询页面，包括但不限于：

- 备件库存查询
- 维修工单查询
- 采购申请查询
- 备件领用查询
- 其他需要保存常用查询条件的页面

---

## 三、数据表设计

### 3.1 表名

`ZBIANSHI`（变式表）

### 3.2 表说明

存储每个用户在每个程序页面下保存的查询变式。每一行代表一个查询条件项；一个变式可对应多行记录（按变式编码 + 程序名 + 用户聚合）。

### 3.3 字段清单

| 序号 | 字段名 | 字段描述 | 字符类型 | 字符长度 | 是否主键 | 字段值来源 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | ZBIANSHI | 变式编码 | CHAR | 100 | 否 | 系统自动生成 | 举例：`liangguoqi-20260923-112301` |
| 2 | ZBIANSHIMIAOSHU | 变式描述 | CHAR | 50 | 否 | 用户输入 | 举例：查询 A 库位冻结库存 |
| 3 | ZCHENGXUMING | 程序名 | CHAR | 100 | 否 | 系统自动抓取 | 当前界面所在程序的程序名 |
| 4 | ZIZIDUAN | 字段 | CHAR | 50 | 否 | 查询条件 | 举例：`LGORT` |
| 5 | ZOPTION | 选项 | CHAR | 2 | 否 | 默认 EQ | 值域：EQ / NE / GT / LT / GE / LE / BT / CP；目前仅实现 `EQ` |
| 6 | ZLOW | 字段值下限 | CHAR | 255 | 否 | 用户输入 | 单值时存下限即可；举例：`1001` |
| 7 | ZHIGH | 字段值上限 | CHAR | 255 | 否 | 用户输入 | 范围值（如日期）需存上下限 |
| 8 | ZUSER | 用户名 | CHAR | 40 | 否 | 当前登录用户 | 举例：`admin` |
| 9 | CREATIONDATE | 创建日期 | DATS | 8 | 否 | 系统生成 | 举例：`2026-09-23` |
| 10 | CREATIONTIME | 创建时间 | TIME | 6 | 否 | 系统生成 | 举例：`11:01:31` |

### 3.4 主键设计建议

因一行记录仅代表一个变式的一个字段条件，建议联合主键/唯一索引：

```sql
-- 唯一索引：同一个用户、同一个程序、同一个变式编码、同一个字段只能有一条条件记录
CREATE UNIQUE INDEX uk_zbianshi ON ZBIANSHI
  (ZUSER, ZCHENGXUMING, ZBIANSHI, ZIZIDUAN);
```

---

## 四、字段业务规则

### 4.1 变式编码（ZBIANSHI）

- **自动生成**，用户不可修改。
- **生成规则**：`用户登录名-创建日期-创建时间`
- **格式示例**：`liangguoqi-20260923-112301`
- **说明**：
  - 日期格式：`YYYYMMDD`
  - 时间格式：`HHMMSS`
  - 生成时机：打开「我的变式」弹窗时生成，点保存时落库；保存成功后立即生成新编码，避免连续保存撞号。
  - 若同一秒内保存多个变式，后端需保证唯一性，可追加序号 `-2`、`-3` 等。

### 4.2 变式描述（ZBIANSHIMIAOSHU）

- **用户手工输入**。
- **必填**，最大长度 50 字符。
- 用于弹窗列表中展示，是用户识别变式的主要字段。

### 4.3 程序名（ZCHENGXUMING）

- **系统自动抓取**当前页面所属程序名。
- 用于隔离不同页面的变式；同一用户在不同页面的变式互不干扰。

### 4.4 字段（ZIZIDUAN）

- 来自当前程序查询条件的字段名。
- 一个变式保存时，需把当前页面所有有值、未置灰、未隐藏的查询字段逐条写入本表。

### 4.5 选项（ZOPTION）

- 目前默认且仅实现 `EQ`（等于）。
- 未来可扩展：
  - `NE`：不等于
  - `GT`：大于
  - `LT`：小于
  - `GE`：大于等于
  - `LE`：小于等于
  - `BT`：介于…之间
  - `CP`：包含，模糊搜索

### 4.6 字段值下限 / 上限（ZLOW / ZHIGH）

- **单值条件**：仅写入 `ZLOW`，`ZHIGH` 为空。
- **范围条件**（如日期区间、数量区间）：`ZLOW` 写入起始值，`ZHIGH` 写入结束值。

### 4.7 用户名 / 创建日期 / 创建时间

- `ZUSER`：取当前登录用户名。
- `CREATIONDATE` / `CREATIONTIME`：取服务器时间落库，分别按 `DATS` / `TIME` 格式存储。

---

## 五、界面设计

### 5.1 入口

在每个查询页面的筛选栏「查询」按钮右侧，放置 **「我的变式」** 按钮（`btn btn-secondary btn-sm`）。

```text
[查询] [重置] [我的变式]
```

### 5.2 弹窗整体布局

点击「我的变式」后弹出 `modal-md` 尺寸弹窗，标题为 **「我的查询变式」**，内部采用**上下分区**布局：

```text
┌─────────────────────────────────────────────────────────┐
│ 我的查询变式                                         [×] │
├─────────────────────────────────────────────────────────┤
│ 【已保存的变式】                                         │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 变式编码      │ 变式名称           │ 默认 │ 操作     │ │
│ ├───────────────┼────────────────────┼──────┼──────────┤ │
│ │ liangguoqi-.. │ 周一备件盘库 [当前] │ 默认 │ 已默认 删除│ │
│ │ liangguoqi-.. │ ERSA 紧急件查询    │      │ 设为默认 删除│ │
│ │ ...           │ ...                │      │ ...        │ │
│ └─────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────┤
│ 【保存当前条件为新变式】                                  │
│ 变式编码              变式名称*              设为默认 [保存]│
│ [系统自动生成]        [                    ] [ ] [保存为新变式]│
├─────────────────────────────────────────────────────────┤
│                                    [关闭]                │
└─────────────────────────────────────────────────────────┘
```

### 5.3 已保存变式区

- **展示形式**：表格。
- **表头**：变式编码、变式名称、默认、操作。
- **行说明**：
  - 每行代表一个用户保存的变式。
  - 当前正在使用的变式行高亮，并在名称后显示「当前」标签。
  - 默认变式在「默认」列显示「默认」标签。
- **行交互**：
  - **点击整行**：应用该变式（回填条件并执行查询），弹窗关闭。
  - **设为默认**（已默认则禁用）：将该变式设为当前页面默认变式，进入页面时自动回填。
  - **删除**：二次确认后软删除该变式。

### 5.4 保存新变式区

- **布局**：单行横向排列，从左到右依次为：
  1. **变式编码**：只读、灰底、不可修改，显示系统自动生成的编码。
  2. **变式名称**：必填，输入框。
  3. **设为默认**：复选框，勾选后保存同时将当前变式设为默认。
  4. **保存为新变式**：按钮，点击保存。

---

## 六、核心业务流程

### 6.1 保存变式

1. 用户在筛选栏输入条件。
2. 点击「我的变式」按钮打开弹窗。
3. 系统生成新的变式编码并显示在变式编码框中。
4. 用户输入变式描述，可选择是否设为默认。
5. 点击「保存为新变式」：
   - 校验变式描述是否为空。
   - 校验当前页面下是否存在同名变式。
   - 校验是否超过每页最大保存数量（建议 20 个）。
   - 将每个有值的字段按行写入 `ZBIANSHI` 表：
     - `ZBIANSHI`：生成的编码
     - `ZBIANSHIMIAOSHU`：用户输入的描述
     - `ZCHENGXUMING`：当前程序名
     - `ZIZIDUAN`：字段名
     - `ZOPTION`：`EQ`
     - `ZLOW` / `ZHIGH`：字段值（范围值存上下限，单值仅存下限）
     - `ZUSER` / `CREATIONDATE` / `CREATIONTIME`：当前用户和时间
6. 保存成功后刷新上方变式列表，清空名称输入框，变式编码框更新为新编码。

### 6.2 应用变式

1. 用户在弹窗中点击某一行变式。
2. 弹窗关闭，系统按 `ZBIANSHI` 查询该变式下所有字段记录。
3. 将各字段值回填到筛选栏对应控件中。
4. 自动执行查询。
5. 标记该变式为「当前」变式。

### 6.3 设置默认变式

1. 用户在弹窗中点击某行的「设为默认」。
2. 当前页面下所有其他变式的默认标记取消，仅该行标记为默认。
3. 用户下次进入该页面时，系统自动回填默认变式的条件（不自动查询）。

### 6.4 删除变式

1. 用户点击某行的「删除」。
2. 弹窗二次确认：「确定删除变式「XXX」吗？」。
3. 确认后执行软删除（建议增加 `DELETED_AT` / `IS_DELETED` 字段，或物理删除按项目规范）。
4. 若删除的是当前正在使用的变式，取消「当前」标记。

### 6.5 进入页面自动回填

1. 用户进入某查询页面。
2. 系统查找该用户 + 该程序名下的默认变式：
   - 存在：自动回填默认变式条件。
   - 不存在：查找最近一次查询的「上次条件」自动回填（临时自动保存，不展示在变式列表中）。
   - 都没有：保持空白筛选条件。

---

## 七、业务规则约束

| 规则项 | 说明 |
| --- | --- |
| 唯一性 | 同一用户、同一程序、同一变式编码只能存在一个变式 |
| 同名校验 | 同一用户、同一程序下变式描述不可重复 |
| 数量上限 | 建议每个用户每个程序最多保存 20 个变式 |
| 空值过滤 | 保存时不存储未填写、已置灰、已隐藏的字段 |
| 默认唯一 | 同一用户、同一程序下只能有一个默认变式 |
| 个人隔离 | 变式按用户名隔离，不可跨用户查看或使用 |

---

## 八、前后端接口建议

### 8.1 查询变式列表

```http
GET /api/query-variant/list?program={ZCHENGXUMING}
```

返回当前用户在某程序下保存的全部变式（聚合后）。

### 8.2 保存变式

```http
POST /api/query-variant/save
```

请求体：

```json
{
  "ZBIANSHIMIAOSHU": "周一备件盘库",
  "ZCHENGXUMING": "spare-parts-stock",
  "isDefault": true,
  "conditions": [
    { "ZIZIDUAN": "werks", "ZOPTION": "EQ", "ZLOW": "1000", "ZHIGH": "" },
    { "ZIZIDUAN": "lgort", "ZOPTION": "EQ", "ZLOW": "1001", "ZHIGH": "" }
  ]
}
```

### 8.3 应用/删除/设置默认

```http
POST /api/query-variant/apply/{ZBIANSHI}
POST /api/query-variant/set-default/{ZBIANSHI}
POST /api/query-variant/delete/{ZBIANSHI}
```

---

## 九、库存查询示例

### 9.1 页面条件

| 字段名 | 字段说明 | 用户输入值 |
| --- | --- | --- |
| `werks` | 工厂 | `1000` |
| `lgort` | 库存地点 | `1001` |
| `matnr` | 物料号 | `60001018` |
| `dateFrom` | 过账日期起 | `2026-09-01` |
| `dateTo` | 过账日期止 | `2026-09-23` |

### 9.2 保存后落库数据

用户 `liangguoqi` 于 `2026-09-23 11:23:01` 保存变式，描述为「查询 A 库位冻结库存」，生成编码 `liangguoqi-20260923-112301`。

| ZBIANSHI | ZBIANSHIMIAOSHU | ZCHENGXUMING | ZIZIDUAN | ZOPTION | ZLOW | ZHIGH | ZUSER | CREATIONDATE | CREATIONTIME |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| liangguoqi-20260923-112301 | 查询 A 库位冻结库存 | spare-parts-stock | werks | EQ | 1000 | | liangguoqi | 20260923 | 112301 |
| liangguoqi-20260923-112301 | 查询 A 库位冻结库存 | spare-parts-stock | lgort | EQ | 1001 | | liangguoqi | 20260923 | 112301 |
| liangguoqi-20260923-112301 | 查询 A 库位冻结库存 | spare-parts-stock | matnr | EQ | 60001018 | | liangguoqi | 20260923 | 112301 |
| liangguoqi-20260923-112301 | 查询 A 库位冻结库存 | spare-parts-stock | dateFrom | EQ | 2026-09-01 | | liangguoqi | 20260923 | 112301 |
| liangguoqi-20260923-112301 | 查询 A 库位冻结库存 | spare-parts-stock | dateTo | EQ | 2026-09-23 | | liangguoqi | 20260923 | 112301 |

> 注：日期区间目前按两个单值字段 `dateFrom` / `dateTo` 存储；未来若支持 `BT` 选项，可合并为一条记录，`ZOPTION=BT`，`ZLOW=2026-09-01`，`ZHIGH=2026-09-23`。

---

## 十、异常处理

| 异常场景 | 处理说明 |
| --- | --- |
| 变式描述为空 | 提示「请输入变式名称」 |
| 同名变式已存在 | 提示「已存在同名变式，请换一个名称」 |
| 超过最大保存数量 | 提示「每个页面最多保存 20 个变式，请先删除不用的」 |
| 变式编码重复 | 提示「变式编码已存在，请关闭弹窗重新打开后再保存」 |
| 删除默认变式 | 删除后该页面无默认变式，下次进入页面不再自动回填 |

---

## 十一、附录：前端实现索引

- 前端框架文件：`V3/js/core/query-variant.js`
- 公共样式：`V3/css/common.css`（搜索 `.qv-` 相关样式）
- 示例页面：`V3/js/pages/spare-parts-stock.js`
- 设计 Demo：`V3/demo-query-variant-styles.html`

---

## 十二、待确认事项

1. 是否需要支持 `BT`（区间）和 `CP`（模糊搜索）选项的界面配置？
2. 变式数量上限是否按 20 个执行？
3. 删除变式采用软删除还是物理删除？当前前端按软删除实现，后端需补充对应字段。
4. 是否需要支持变式排序（拖拽 / 手动排序）？

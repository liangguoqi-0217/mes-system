# 采购申请 PRD

---

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | MES 系统 - 采购申请模块 PRD |
| 版本 | V1.0 |
| 创建日期 | 2026-08-06 |
| 最后更新 | 2026-08-06 |

---

## 2. 模块概述

### 2.1 模块定位

采购申请是 MES 系统与 SAP 系统之间的桥梁模块。需求部门在 MES 端创建采购申请，通过接口传输至 SAP，SAP 调用 BAPI 创建成功后返回采购申请号，MES 将数据写入 EBAN 底表。后续采购员在 SAP 端依据采购申请创建采购订单（PO），MES 端可查询执行进度。

### 2.2 核心流程

```
需求部门创建采购申请 → MES调SAP创建接口 → SAP返回采购申请号 → MES写入EBAN底表
                                                                    ↓
需求部门查询执行进度 ← 采购员在SAP创建PO ← SAP回传PO信息(可选)
```

### 2.3 范围边界

- **在 MES 管理**：采购申请的创建、查询、修改、导出
- **不在 MES 管理**：采购订单（PO）的创建与管理，PO 仅在 SAP 端操作
- **审批流**：采购申请和采购订单均无审批流
- **统计口径**：以行项目为单位，"已结算"标识由业务部门手动打标

---

## 3. 用户场景与入口

### 3.1 目标用户

- 每个部门指定 1~2 名人员拥有创建和修改权限
- 其他用户仅可查看（后续扩展）

### 3.2 创建入口

#### 入口 1：手动创建
- 点击「创建采购申请」按钮 → 弹出创建表单
- 逐一添加行项目，填写完毕后点击「提交」→ 调 SAP 创建接口

#### 入口 2：批量创建（从红灯物料预警）
- 在红灯物料预警页面勾选物料（上限 100 行）
- 点击「批量生成采购申请」→ 带入行项目列表
- 用户可继续手工调整每一行
- 超过 100 行时限制勾选，提示"数量太多，请分开建单"

---

## 4. 数据模型

### 4.1 抬头层（Header）

| 字段名 | 接口字段 | 类型 | 必填 | 来源/说明 |
|---|---|---|---|---|
| 采购申请凭证类型 | BBSRT | CHAR:4 | 是 | 固定值 Z02，不可修改 |
| 工厂 | WERKS | CHAR:4 | 是 | 默认值，用户可修改 |
| 部门 | ZBUMEN | CHAR:30 | 是 | 根据登录用户自动带出，不可修改 |
| 防重唯一码 | ZNUMBER | CHAR:32 | 是 | MES 系统自动生成（UUID），后台字段，不在前台展示 |
| 预留字段 1-5 | — | — | 否 | 当前阶段不使用 |

### 4.2 行项目层（Line Item）

| 字段名 | 接口字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| 行项目号 | BNFPO | NUMC:5 | 是 | 系统自动生成，按 10、20、30… 递增 |
| 工厂 | WERKS | CHAR:4 | 是 | 与抬头一致 |
| 申请数量 | BAMNG | QUAN | 是 | |
| 单位 | MEINS | UNIT:3 | 是 | 下拉选择 |
| 交货日期 | LFDAT | DATS:8 | 是 | 日期选择器 |
| 创建者 | ERNAM | CHAR:12 | 是 | 当前登录用户账号，后台传输，前台不展示 |
| 创建日期 | ERDAT | DATS:8 | 是 | 系统自动，后台传输，前台不展示 |
| 创建时间 | ERZET | TIMS:6 | 是 | 系统自动，后台传输，前台不展示 |
| 申请人姓名 | AFNAM | CHAR:12 | 是 | 前台手动选择，下拉数据来源为本部门用户列表 |
| 科目分配类别 | KNTTP | CHAR:1 | 是 | 可选值：空/K/F（详见 4.3） |
| 物料编号 | MATNR | CHAR:18 | 条件必填 | 见 4.3 联动规则 |
| 短文本 | TXZ01 | CHAR:40 | 条件必填 | 见 4.3 联动规则 |
| 物料组 | MATKL | CHAR:9 | 条件必填 | 见 4.3 联动规则，下拉选择 |
| 存储地点 | LGORT | CHAR:4 | 否 | 下拉选择 |
| 评估价格 | ZPRICE | DEC | 否 | 用户手动填写 |
| 建议供应商 | LIFNR | CHAR:10 | 否 | 下拉框选择 |
| 供应商物料编号 | IDNLF | CHAR:35 | 否 | 纯文本输入 |
| 成本中心 | KOSTL | CHAR:10 | 否 | 下拉选择 |
| 总账科目 | SAKTO | CHAR:10 | 否 | |
| 内部订单 | AUFNR | CHAR:12 | 否 | |
| 备注 | SGTXT | CHAR:50 | 否 | 文本输入 |
| 已结算标识 | EBAKZ | CHAR:1 | 否 | 值域：空/X。业务手动打标，不可自动设置 |

### 4.3 科目分配类别（KNTTP）联动规则

#### KNTTP = 空（正常物料采购）

| 字段 | 行为 |
|---|---|
| 物料编号 | **必填** |
| 物料组 | **置灰**，由物料主数据自动带出 |
| 短文本 | 自动带出，可修改 |
| 内部订单 | 隐藏 |

#### KNTTP = K（费用化采购）

| 字段 | 行为 |
|---|---|
| 物料编号 | **置灰**，不可填写 |
| 物料组 | **必填**，下拉选择 |
| 短文本 | **必填**，手动输入描述 |
| 内部订单 | 隐藏 |

#### KNTTP = F（订单采购）

| 字段 | 行为 |
|---|---|
| 内部订单 | **必填**（AUFNR），手动输入 |
| 物料编号 | 可选填写 |
| 物料组 | 用户未输入物料时 **必填** |
| 短文本 | 用户未输入物料时 **必填** |

---

## 5. 功能需求

### 5.1 创建采购申请

**流程：**
1. 点击「创建采购申请」
2. 弹出选择弹窗（手动创建 / 批量创建）
3. 进入对应创建流程
4. 填写完毕 → 点击「提交」→ 调 SAP 创建接口
5. SAP 返回采购申请号 → MES 写入 EBAN 底表
6. 用户手动刷新页面后看到新单据

**行项目规则：**
- 最少 1 行，最多 100 行
- 行项目号自动递增生成（10 → 20 → 30…）
- KNTTP 驱动字段联动（见 4.3）
- 申请人 AFNAM 下拉数据为该部门用户列表
- 提交后无草稿/暂存状态，不提供"保存"按钮

### 5.2 查询列表

查询条件（共 9 个）：

| 序号 | 查询条件 | 控件类型 |
|---|---|---|
| 1 | 工厂 | 下拉 |
| 2 | 采购申请号 | 文本输入 |
| 3 | 物料 | 文本输入 |
| 4 | 交货日期（区间） | 日期范围选择 |
| 5 | 创建月份（单选） | 月份选择（选中某月，展示该月全部申请） |
| 6 | 成本中心 | 下拉 |
| 7 | 是否结算 | 下拉（空/已结算） |
| 8 | 处理状态 | 下拉（全部/未编辑/已创建采购订单） |
| 9 | 申请人 | 文本输入 |

### 5.3 列表页行项目展示

列表页展示采购申请的行项目明细，以行项目为粒度展示。

**展示字段（约 19 列 + 操作列）：**

| 列名 | 说明 |
|---|---|
| 工厂 | 合并单元格（同一抬头行合并） |
| 采购申请号 | 合并单元格 |
| 行项目 | |
| 物料 | |
| 短文本 | 超长省略号 + title 悬浮显示 |
| 存储地点 | |
| 申请人 | |
| 创建日期 | 单据创建日期 |
| 科目分配类别 | |
| 交货日期 | |
| 数量 | 右对齐，千分位 |
| 单位 | |
| 处理状态 | 徽章：未编辑（灰）/ 已创建采购订单（蓝），依据行项目 status 字段（N/B） |
| 已结算 | 徽章：是（绿）/ 否（灰） |
| 评估价格 | 右对齐，保留两位小数 |
| 预估总金额 | 右对齐（= 评估价格 × 申请数量） |
| 操作 | 「查看」按钮 + 「刷新采购进度」按钮 |

**交互优化：**
- 冻结"工厂"和"采购申请号"两列，横向滚动时始终可见
- 紧凑样式（dense table），减小内边距和字号
- 操作列保留「查看」和「刷新采购进度」两个按钮，以 `.table-actions`（flex）容器并排排列，不换行

**刷新采购进度（新增功能）：**
- 点击「刷新采购进度」→ MES 调用 SAP 查询接口（见 8.3），获取该采购申请最新执行进度
- 接口调用成功 → 弹窗提醒「采购进度已获取」
- 接口调用失败 → 弹窗提醒「数据调取失败」
- 请求期间按钮置灰禁用，防止重复点击

### 5.4 查看弹窗

点击「查看」按钮 → 弹出大弹窗（宽度 90vw, max-width: 1200px+）。

**展示内容：**
- **抬头信息**：工厂、采购申请凭证类型（Z02）、采购申请号、创建日期、创建时间、部门
- **行项目表格**：完整行项目字段（含预估总金额）

#### 弹窗内编辑

弹窗内设有「编辑」按钮，点击后弹窗切换为编辑模式：

##### 可编辑条件
| 行项目状态 | 可操作 |
|---|---|
| 未转 PO | 所有字段可修改 |
| 已转 PO（有 PO 号） | 只能打"已结算"标识 |

##### 编辑规则
- 原位编辑：行项目字段直接变为 `<input>/<select>` 控件
- 可添加新行：点击「+ 添加行」在列表末尾新增，用户逐行填写
- 已生成采购申请号后不可删行：通过打"已结算"（EBAKZ = X）标记关闭行项目
- 点击「提交修改」→ 调 SAP 修改接口 → SAP 返回成功后更新 EBAN 底表

### 5.5 导出 Excel

列表页提供「导出」按钮，导出当前筛选条件下的采购申请列表为 Excel 文件。

### 5.6 不可删除整单

不支持删除整个采购申请单据。如需关闭某行，通过打"已结算"标识（EBAKZ = X）实现。

---

## 6. 交互规范

### 6.1 列表页

- 按钮样式：`btn btn-blue btn-sm`（蓝色按钮，`#3B82F6`）；「刷新采购进度」用 `btn btn-teal btn-sm`（青绿色按钮 `#0D9488`，hover `#0F766E`，白色文字），与「查看」形成明显色彩区分
- 操作列保留「查看」+「刷新采购进度」两个按钮，并排排列不换行
- 冻结"工厂"/"采购申请号"两列
- 紧凑表格样式，控制总宽度
- 表格可横向滚动，固定表头

### 6.2 弹窗

- 宽度尽量大（90vw, max-width ≥ 1200px）
- 弹窗内布局充分利用空间
- 只读模式与编辑模式通过「编辑」/「提交修改」按钮切换
- 行项目原位编辑（`<span>` → `<input>`/`<select>`）

### 6.3 创建入口

- 点击「创建」→ 弹出选择弹窗（手动创建 / 批量创建）→ 进入对应流程
- 不直接跳转表单页

### 6.4 字段联动

- KNTTP 变化 → 实时联动物料号/物料组/短文本/内部订单的灰填状态和必填标识
- 物料号输入/选择 → 自动带出短文本、物料组、单位等信息（从物料主数据）

---

## 7. 业务规则

| 规则编号 | 规则描述 |
|---|---|
| BR-01 | 行项目最少 1 行，最多 100 行，超出提醒"数量太多，请分开建单" |
| BR-02 | 行项目号自动递增：10、20、30… |
| BR-03 | 采购申请凭证类型固定 Z02，不可修改 |
| BR-04 | 部门 ZBUMEN 根据登录用户自动带出，不可修改 |
| BR-05 | 创建者 ERNAM 为当前登录用户账号，后台传 SAP，前台不展示 |
| BR-06 | 创建日期/创建时间系统自动生成，后台传 SAP，前台不展示 |
| BR-07 | ZNUMBER 由 MES 生成唯一码，用于接口防重校验，后台字段 |
| BR-08 | 提交后无草稿状态，不提供"保存"按钮 |
| BR-09 | 提交/修改后需手动刷新页面才能看到最新数据 |
| BR-10 | 已转 PO 的行项目不可修改数量等信息，只能打"已结算"标识 |
| BR-11 | 未转 PO 的行项目所有字段可修改 |
| BR-12 | 已生成采购申请号后不可删除行，只能通过 EBAKZ = X 关闭 |
| BR-13 | 不支持删除整单 |
| BR-14 | EBAKZ 不自动设置，由业务部门手动标记（SAP 和 MES 均不自动打 X） |
| BR-15 | 批量创建从红灯预警勾选物料上限 100 行 |
| BR-16 | 批量创建带入的行仍可手工修改 |
| BR-17 | 申请人 AFNAM 下拉数据来源为当前部门的所有用户 |
| BR-18 | 建议供应商为下拉框选择，供应商物料编号为纯文本输入 |

---

## 8. 接口说明

### 8.1 创建接口

**方向**：MES → SAP

**触发时机**：用户点击「提交」

**传输数据**：抬头 + 行项目（含 ZNUMBER 防重码）

**返回数据**：SAP 返回采购申请号（如 1800000319）

### 8.2 修改接口

**方向**：MES → SAP

**触发时机**：用户点击「提交修改」

**传输数据**：采购申请号 + 修改后的行项目数据

**返回数据**：SAP 返回修改成功确认

### 8.3 SAP 回传信息（查询）

MES 可查询以下来自 SAP 的字段（用于展示采购申请执行进度）：

| 字段 | 说明 |
|---|---|
| 采购订单号 | SAP 创建的 PO 号 |
| PO 行项目 | PO 对应的行项目号 |
| 订货数量 | PO 中的订货数量 |

**触发方式**：列表页点击「刷新采购进度」按钮调用本查询接口；调用成功后弹窗提醒「采购进度已获取」，失败则提醒「数据调取失败」。

---

## 9. 附录

### 9.1 科目分配类别（KNTTP）枚举

| 值 | 含义 | 说明 |
|---|---|---|
| 空 | 正常物料采购 | 物料号必填 |
| K | 费用化采购（成本中心） | 物料号置灰 |
| F | 订单采购 | 内部订单必填 |

### 9.2 采购申请凭证类型（BBSRT）

固定值：**Z02**（该字段值域存在多种类型，但现阶段仅使用 Z02）

### 9.3 已结算标识（EBAKZ）枚举

| 值 | 含义 |
|---|---|
| 空 | 正常（未结算） |
| X | 已结算（业务关闭该行） |

### 9.4 物料主数据

原型阶段使用 Mock 数据（30 条），字段包括：

`matCode, shortText, matGroup, storageLocation, price`

上线后由 SAP 正式推送物料主数据至 MES。

### 9.5 Mock 下拉数据源

- **成本中心**：Mock 数据
- **物料组**：Mock 数据
- **存储地点**：Mock 数据
- **建议供应商**：Mock 数据
- **申请人**：本部门用户列表（Mock）
